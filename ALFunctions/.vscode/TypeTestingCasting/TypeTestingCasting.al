interface MyInterface
{
    /* procedure TestInterface(intf: Interface MyAddressProvider);
     begin
         if intf is MyAddressProvider then
             Message('The interface is of type MyAddressProvider.')
         else
             Message('The interface is not of type MyAddressProvider.');
     end;*/
}